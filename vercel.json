{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "hono",
  "buildCommand": "pnpm run build",
  "outputDirectory": "src",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
